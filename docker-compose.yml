services:
  mcp-agent-mail:
    build: .
    image: mcp-agent-mail:latest
    container_name: mcp-agent-mail
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - RUST_LOG=info
